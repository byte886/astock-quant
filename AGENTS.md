# AGENTS.md · 项目级 AI 操作手册

> 本文件是给 AI 看的指令性操作手册。与全局 `~/Doubao/AGENTS.md` 冲突时，以本文件为准（仅本项目内）。

## 1. 项目定位与边界

> **两条主线（后续所有沟通的顶层框架；2026-09-21 据 `docs/HANDOFF.md` §12 补记，原话以 HANDOFF 为准）**
> 本项目服务一位职业炒股客（下称"**操盘手/牵头人**"），他"已经在用豆包但用得很散"。整体分两条**并行、互为输入**的线，后续任何需求先判断落在哪条线、是否两线都要动：
>
> - **① 量子系统（本仓库 = 量化工具线）**：ETF 轮动 → 多因子选股，用免费开源数据（Baostock）做中低频（月度调仓）回测，跑赢沪深300 后进模拟盘。追求客观、可回测、纪律化，**不替人做投资决策、不承诺收益**。
> - **② 外脑（AI 交易外脑 = 克隆操盘手能力线）**：按"CEO 外脑"方法论，把操盘手感性、说不清的交易判断采出来，建一个**持续更新的专属交易档案**，让 AI 不用他每次重复持仓/成本/风格就能答得贴他。怎么落地看 `docs/外脑落地SOP.md`，采什么看 `docs/操盘手交易判断采集清单.md`，沟通纪律与人物画像看 `docs/参与者画像.md`，原始素材在 `09_调研底稿与素材/操盘手经验/`。
> - **两线关系**：外脑的每日复盘、板块判断、操作风格 → 量子系统的情绪面/因子输入与策略假设来源；量子系统的数据、回测、统计 → 外脑判断的客观校验。两者不是一回事，不要把"采操盘手的经验"当成"写量化策略"，也不要用量化回测结果替代操盘手的主观体系。
> - **操盘手每日 A 股复盘是外脑的固定持续输入**：拿到后按 `09_调研底稿与素材/操盘手经验/` 既有规范归档（`NNN_<豆包threadid>_<标题> - 豆包.md`，编号顺延），并提炼其中可结构化的判断（板块、量能、驱动、风格切换）供外脑档案与量子系统因子参考。
> - **素材本地检索手段（补全历史沟通时用）**：操盘手的**豆包对话**用 `local-trajectory-recall` 技能跨会话/跨 Profile 检索（用法见本文件 §7）；操盘手相关的**微信沟通、公众号文章**用 `wechat-control` 技能只读解密本机微信库（`wx search/history/biz-articles`，只读零风控，微信锁 4.1.8）。trajectory/微信库原文只本机自用，归档只取复盘/观点正文，不带系统提示、token、密钥。
> - 内容采集与知识库底座（视频号/公众号/B站等多平台采集、转写、OCR、行业知识库）在姊妹仓 `~/Desktop/multiplatform-content-pipeline/`，本仓库只用量化所需的结构化结果，不重复造采集流水线。

- 这是一个 **A 股量化交易研究项目**，当前处于 **G2 验证门**（D 多因子策略最小验证，详见 `docs/WORKFLOW.md` §0 阶段路由）。
- 核心目标：用价值(股息率)+成长(ROE)多因子策略跑历史回测，与沪深300对比，验证是否成立；成立后进模拟盘。
- **不做**：不替用户做投资决策、不承诺收益、不写高频交易、不碰券商闭源终端（QMT/PTrade）。

## 2. 目录约定

| 目录 | 放什么 | 禁止 |
|---|---|---|
| `00_项目总纲.md` | 项目章程，更新状态/进度 | 不要写细节讨论 |
| `01_结论与产出/` | 最终定稿的报告/方案 | 不放草稿 |
| `02_决策记录/` | ADR（决策+理由+日期）、治理卡 | 不放待办 |
| `03_进行中的任务/` | TASK_STATUS.md、ISSUES.md | 不放最终文档 |
| `09_调研底稿与素材/` | 原始资料、视频文稿、引用链接 | 不放结论性文档 |
| `src/` | 源代码（当前阶段基本为空） | 调研期不要大规模写代码 |
| `data/` | 数据文件（不入库） | 不要提交到 git |
| `notebooks/` | 研究用 Jupyter | 命名加日期前缀 |

## 3. 工具用法与坑

### 3.1 数据来源

| 数据源 | 用途 | 是否免费 | 凭证 | 状态 |
|---|---|---|---|---|
| Baostock | A股日线/周线/月线、财务数据、复权因子 | 是（注册免费） | 无需凭证，login 即可 | 规划中，待下载 |
| AkShare | 补充数据（龙虎榜、资金流、舆情） | 是 | 无需凭证 | 备选 |
| Tushare | 可选（高质量数据） | 免费版有积分限制 | 环境变量 TUSHARE_TOKEN | 暂不使用 |
| 豆包 API | AI 舆情因子/文本分析 | 按量付费 | 环境变量 DOUBAO_API_KEY | 选型中 |

### 3.2 关键约定

- **股票代码格式**：baostock 用 `sh.600519` / `sz.000001`；内部统一用 6 位数字 + 交易所前缀
- **数据文件格式**：原始数据用 CSV（baostock 默认），处理后用 parquet（压缩率高、读取快）
- **数据更新频率**：每日收盘后下载当日数据（盘后 16:00 后）
- **回测默认参数**：初始资金 100 万、手续费万 2.5（双边）、滑点 0.1%、用前复权
- **候选池**：待定（全 A 股 vs ETF 小池）
- **策略方向**：中低频多因子选股 / ETF 轮动（待对比确定）
- **执行方式**：盘后信号 + 人工下单 起步，后续评估自动下单

### 3.3 工具坑

- **视频提取**：用 `yt-dlp`，B 站/抖音无字幕时用音频转写兜底。
- **Baostock**：需先 `login()`，使用完 `logout()`；免费版有调用频率限制，单只股票间隔 2-3 秒防封。
- **大模型 API**：密钥放环境变量或 `config/local_*.py`（已 gitignore），不要写进代码。
- **回测框架**：选型未定时，不要在任何一个框架上投入大量开发。
- **FunASR**：首次下载模型需联网（视频转写用，非量化核心）。

### 3.4 外部参考资源

- 高顿 CPA 知识库：`~/Doubao/chats/2026-08-26/new-chat/gaodun-course-knowledge-base/`（方法论参考：三层解耦、四阶段流水线、ADR驱动）
- 内容流水线：`~/Desktop/multiplatform-content-pipeline/`（含 domains/stock/ 股票领域，内容采集+知识提取参考）
- 项目管理技能：`~/Doubao/skills/project-manager/`（本项目的结构和模板来源）

## 4. 禁止事项

- ❌ 不要修改 `09_调研底稿与素材/` 下的原始文稿（提取后只读）。
- ❌ 不要把 API key、token、账号密码写进任何会入库的文件。
- ❌ 不要在调研期替用户决定框架选型——只做对比，决策留给用户。
- ❌ 不要给出具体买卖建议或收益承诺。
- ❌ 不要删除用户已有的文件，覆盖前先确认。

## 5. 新会话恢复顺序

**冷启动（首次接触/跨阶段切换）**：
1. 读本文件（AGENTS.md）— AI 操作规则
2. 读 `README.md` — 项目概览
3. 读 `00_项目总纲.md` — 目标/状态/治理
4. 读 `docs/DOCUMENTATION_MAP.md` — 文档地图（按需沿链接深读）
5. 读 `docs/HANDOFF.md` —— 项目怎么走到今天（§1 倒序日志）+ 现在纠结什么（§3）；**这是活文档，本轮只要有重要讨论/决策，就要往 §1 倒序追加一条**
6. 读 `docs/WORKFLOW.md` **§0「AI项目经理·阶段路由」** —— 先定位当前阶段(G1/G2/G3)和我的角色，定位错了全跑偏；再按需读其余技术流水线阶段
7. 读 `03_进行中的任务/TASK_STATUS.md` — 当前进度
8. 读 `03_进行中的任务/ISSUES.md` — 开放问题
9. 按需读 `09_调研底稿与素材/` — 具体资料

**续接（接着做同类活）**：
1. 只读本文件（AGENTS.md）扫规则
2. 读 `TASK_STATUS.md` + `ISSUES.md`（到哪、下一步、卡在哪）
3. 读该环节那一篇 SOP + 上一单元样板成品

> 判不准走冷启动还是续接时，就高走冷启动。

## 6. 提交前检查清单

- [ ] 没有密钥/敏感信息入库
- [ ] `data/` 和 `results/` 下的大文件未入库
- [ ] TASK_STATUS.md 已更新当前进度
- [ ] 新决策已写入 `02_决策记录/`
- [ ] **本轮若有重要讨论/决策 → 已往 `docs/HANDOFF.md` §1 倒序追加一条日志**（项目日志是活的，别让它停在旧状态）
- [ ] 原始调研素材未被修改

## 7. 本地会话轨迹（trajectory）——压缩后回查原始记录

**问题**：会话变长后，早期逐字细节被压成摘要，上下文里只剩结论、没有原话。

**解法**：用独立技能 `local-trajectory-recall` 的 `recall.py`（本机 trajectory 检索，跨会话）。

> **豆包专属能力**：依赖豆包桌面端把会话逐字落盘到本机 `trajectory.jsonl`；别的 AI 助手/平台既无此文件、格式也不同，装了也跑不出东西。仅 macOS 实测。
脚本在 `~/Doubao/skills/local-trajectory-recall/scripts/recall.py`，下面记作 `<recall>`。

- 列历史会话：`python3 <recall> list`（跨会话找"之前聊过什么"先看这个）
- 定位当前会话：`python3 <recall> where`
- 统计：`python3 <recall> stats`
- 搜历史：`python3 <recall> search 关键词`（当前会话）；`--all` 跨全部主会话；`--id <sid片段>` 指定会话
- 导出：`python3 <recall> export out.md`

> 项目根 `scripts/session_history.py` 是早期单会话版，保留可用；上面的 recall.py 是跨会话增强版，优先用它。

**规则**：
- 上下文被压缩、记不清"当时到底怎么说的"时，回这里查原文，**别凭印象编**。
- trajectory 含系统提示、工具返回、token 等敏感内容——只本机自用，**绝不粘进 git、文档、对外聊天**。
- 它是原始流水账；整理过的结论仍以 HANDOFF / 参与者画像 / REQUIREMENTS 为准，trajectory 只用于对原文。

### 7.1 操盘手其它任务窗口 · 增量采集（A 方案，记忆触发、不挂定时）

- 脚本：`python3 scripts/scan_operator_sessions.py`。增量扫描操盘手在**其它工作任务窗口**（非当前会话）
  新产生的复盘/行情/操作观点，靠游标续读（不从头扫）、去重，暂存到 `data/operator_recall/inbox/`。
  完整流程、归档编号、结构化提炼模板见 **`docs/外脑-操盘手会话增量采集SOP.md`**。
- **触发时机（不设 cron，靠记忆）**：① 新会话/新窗口接手本仓、走「新会话恢复顺序」时顺带跑一次；
  ② 用户提到操盘手/大海星/nero 在别的窗口聊了复盘或行情、让"扫一下他的会话"；③ 更新外脑档案前。
  跑完按 SOP：审阅 inbox（剔除项目复盘/开发讨论等误召回）→ 在 `09_调研底稿与素材/操盘手经验/`
  归档编号（当前最大 125，新档从 **126** 起）→ 提炼指数/量能/板块/驱动/操盘手观点与操作/风格。
- **边界**：只覆盖 agent 工作任务 trajectory；豆包 2.1 Pro **普通对话**正文在云端、不落本机，
  须走 `doubao.com/thread/<id>` 分享链接读取。脚本只在**操盘手实际使用的那台机器**上能扫到他的会话
  （trajectory 不跨机同步）。`data/operator_recall/` 已 gitignore，原文绝不入 public 仓。
