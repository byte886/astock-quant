# 126 · 操盘手在另一台机推进"3号股票池量能扫描器"（过程记录 + 跨机协作）

> 编号：126
> 日期：2026-09-21
> 来源：**用户截图喂入**（操盘手在另一台 Windows 机、同一豆包账户、任务"上周五A股走势复盘"里的执行过程），非 thread 链接。
> 类型：工具化诉求 + 跨机协作事件（业务新认知少，主要是他开始主动要"把 1/2/3 号体系自动化"）
> 机器边界：操盘手那台是 Windows（路径 `C:\Users\Mac\Doubao\chats\2026-09-21\new-chat\astock-quant`），其会话 trajectory **不跨机同步**，本机（Mac）只能通过用户截图/共享链接获得。

---

## 一、他在那台机让 AI 做了什么

1. 从 GitHub 拉取 `byte886/astock-quant`（仓库很小，约 1.2MB / 201 个文件，不含行情大数据）。
2. zip / raw 下载通道超时、raw 域名被 DNS 污染，改用 **api.github.com Contents API** 逐个取关键文档（策略、ADR、任务状态、3 号股票池素材、模拟盘脚本）。
3. 通读操盘手素材，**对齐编号体系**（结论与本仓经验 003/004 原文一致）：
   - 1 号策略 = 左侧困境反转；2 号策略 = 右侧突破波段；3 号 = 量能筛选股票池（注意那台机 AI 有一处把 2 号写成"底部突破"，**以经验 004 原文"右侧突破波段"为准**）。
   - 3 号池归档清单 **55 个标的（53 只个股 + 2 只 ETF）**，9-10 基准约 52 只个股 + 2 只 ETF。
4. 写了一个批量扫描脚本 **`pool3_scan.py`（+117 行）**：内置 55 个标的，指标含 **TRIX、均线、量比、突破、试盘、位置、支撑/压力**全套；目标是用截至上周五（9-18）收盘的日线，套规则给每只标的评级。
5. 环境：Windows + Python 3.14，先装 akshare/pandas（清华源）；**akshare 的东方财富接口在该机被断连**，改用项目主数据源 **Baostock**；在 `pip install baostock` 时"执行环境疑似被安装进程占用"，反复探测、**任务在此打转**（机器随后被关）。

## 二、关键事实 / 风险

- ⚠️ **`pool3_scan.py` 只存在于那台 Windows 机的本地工作目录，没有 commit、没有 push**，GitHub 与本 Mac 主仓都没有（已核实 `scripts/` 下无此文件）。机器关机期间拿不到这 117 行；下次那台机开机应先让它 `git add/commit/push`，或把文件内容带回，避免白写。
- 那台机**没有本地行情大数据**（仓库不含 data/），它本来打算现拉 baostock；而 **Mac 主仓已装好 baostock、中证800日线已全、全市场日线正在后台自愈下载**，3 号池 55 标的（沪市为主）的日线大概率已在本地——**在 Mac 上接手跑这个扫描器比在 Windows 上重搭环境更顺**。
- 操盘手的真实诉求：把他手工的"3 号池量能粗筛 → 1/2 号策略细判"流程**程序化、每日自动给评级**。这与本仓 D 多因子/模拟盘是互补关系（他的 1/2 号是技术+资金面打法，D 是基本面因子打法），不是重复造轮子。

## 三、对项目的待办（接手选项）

- 跨机待办已登记到 `03_进行中的任务/TASK_STATUS.md`：
  - A. 在 Mac 主仓按经验 003/004 权威口径重写一个最小可用 `pool3_scan.py`，直接用本地 baostock 日线对 55 标的跑一次 9-18 收盘评级（不依赖那台机）；
  - B. 等 Windows 机开机、取回原版 117 行对照合并后再跑；
  - C. 暂不开发，只把口径沉淀好（本次已完成沉淀），等主线（价值模拟盘）告一段落再做。
- 无论哪种，扫描器产出的"信号"都只是研究参考，**不构成投资建议、不自动下单**（项目红线）。

## 四、可复用的指标口径（那台机 AI 归纳，待对照原文确认后实现）

- 量能：`VOL / HHV(VOL,60)`，45%～65% 为缩量观察区（3 号池规则）。
- 1 号：底部区间 + 业绩减亏/拐点 + TRIX 低位金叉 + 放量试盘 + 缩量磨底，左侧分批、强止损、不追高。
- 2 号：放量突破关键压力 + 增量资金持续 + 均线多头 + 板块催化，无突破不介入。
- TRIX、支撑/压力位的精确算法实现前需与操盘手确认阈值，不能由 AI 自行定参。
