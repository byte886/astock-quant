# TASK_STATUS · 活跃任务台账

> 最后更新：2026-09-21
> 本文件是"现在做到哪、下一步是什么"的唯一真相源。任务开始/完成/遇阻塞时立即更新。

## 战略方向（2026-09-19 拍板）

ETF 轮动初轮效果不佳（年化约 2.4%、最大回撤 -38%）后，确定**同时做三方向，执行顺序 C → D → B**：

1. **C 舆情因子**（股吧散户情绪反向指标，差异化特色）— 初步验证完成，按用户 **C2 决定挂起**，转 D。
2. **D 多因子选股**（价值=股息率；成长=净利润增速+ROE门槛）— **当前主线**。
3. **B 模拟盘**（承载所有策略信号的运行框架）— 最后做。

## 当前阶段

✅ **D 多因子正式回测已出（2026-09-20）。G2 验证门决策：价值 Conditional Go（进模拟盘）、成长 Hold（回撤过大待改进）。**

## G2 验证门决策（2026-09-20）

回测 2015-01 ~ 2026-09，月度、15只等权、含成本、PIT 无未来函数（已逐行验代码）：

| 策略 | 年化 | 沪深300年化 | 超额 | 最大回撤 | 夏普 | 决策 |
|---|---|---|---|---|---|---|
| 价值(高股息) | 13.0% | 4.0% | +9.0% | -35% | 0.62 | **Conditional Go** → 进模拟盘用实时数据验证 |
| 成长(YOYNI+ROE) | 8.0% | 4.0% | +4.0% | -60% | 0.34 | **Hold** → 回撤过大，加风控/重审因子后再议 |

- 保留项（诚实）：当前用**现中证800成分股**往回看，有已知幸存者偏差（策略文档§8），上模拟盘前用实时数据复核。
- 结果文件：`results/multifactor_backtest/`（净值/回撤/年度收益/绩效汇总/报告图）。

## 进行中（In Progress）

| # | 任务 | 负责人 | 状态 |
|---|---|---|---|
| T21 | 中证800财务数据下载（profit/growth/dividend） | AI/launchd | ✅ **706/706 完成**（profit/growth 各706、dividend 704、0 空文件） |
| T22 | D 多因子回测系统 | AI | ✅ **正式回测已出**（见上表 G2 决策） |
| T27 | 价值策略进模拟盘（B线启动） | AI | 🟡 下一步：搭模拟盘框架（100万虚拟、次日开盘、13:30复盘、盘后微信推信号） |
| T28 | 成长策略回撤归因与改进 | AI | ⬜ Hold：分析 -60% 回撤来源，加择时/止损或换因子 |
| T13 | 全量A股日线+5分钟线下载 | AI/launchd | 🟢 **后台自愈下载中**（2026-09-21 重构，见下节）。ETF 5分钟线 100%；个股日线/分钟线以磁盘为准持续补齐，断点续传，崩溃/重启自动续跑，无需人工催 |

## D 回测系统（T22）已完成的代码

| 模块 | 文件 | 作用 |
|---|---|---|
| 数据层 | `src/quant/data.py` | 中证800日线面板、profit/growth/dividend、基准、月末交易日 |
| 因子层 | `src/quant/factors.py` | PIT 股息率（连续3年分红+TTM）、YOYNI+ROE≥10%门槛 |
| 引擎 | `src/quant/engine.py` | 日频盯市、月末信号次月开盘调仓、涨停买不进/跌停卖不出/停牌、佣金万2.5+卖出印花税千1+滑点千1 |
| 绩效 | `src/quant/metrics.py` | 年化/波动/最大回撤/夏普/卡玛/月度胜率/超额/换手 |
| 可视化 | `src/quant/viz.py` | 净值+回撤+年度收益三联图（中文字体已修复 Heiti SC） |
| 入口 | `scripts/run_multifactor_backtest.py` | `--factor value/growth/both --top 15 --start 2015-01-01` |

- 已用 16 只早期银行股（财务数据未下全）跑通：140 次调仓、引擎逻辑正确；价值选出全是银行符合因子逻辑，但**池子极小、结果无统计意义，非正式结论**。
- 数据下完后执行：`.venv/bin/python scripts/run_multifactor_backtest.py`，结果在 `results/multifactor_backtest/`。

## 下一步（数据下完后）

1. 重跑正式回测，产出价值 vs 成长 vs 沪深300 的净值曲线/回撤/年度收益。
2. 向用户解读结果，回答：两策略是否跑赢沪深300？
3. 参数敏感性（持仓数、ROE门槛、回测起点）→ 决定是否进模拟盘。
4. 历史动态成分股（v0.2 消除幸存者偏差）：`scripts/download_fundamentals.py --constituents`。

## 待办（Backlog）

### 需求讨论（聊清楚后更新到 REQUIREMENTS.md）

| # | 议题 | 状态 |
|---|---|---|
| D-02 | 价值策略细节 | ✅ 已定稿（见策略文档：中证800剔科创板、月度、15只等权、TTM股息率、连续3年分红） |
| D-03 | 成长策略细节 | ✅ 已定稿（口径①：最新财报YOYNI排名 + 最新年报ROE≥10% + 净利润为正） |
| D-04 | 多因子候选池 | ✅ 已定稿（中证800=沪深300+中证500，剔科创板=706只） |
| D-09 | 财务数据需求 | ✅ 已定稿（baostock，profit/growth/dividend，严格PIT用pubDate/除息日） |
| D-12 | 回测可视化 | ✅ 已定稿（matplotlib 净值+回撤+年度收益，已实现） |
| D-01 | 模拟盘设计 | 🟡 框架已定（100万虚拟、次日开盘成交、13:30复盘、盘后微信推信号），**待开发** |
| D-10 | 微信推送通道 | ⬜ 待选（Server酱/企业微信机器人） |
| D-11 | 每日复盘内容与格式 | ⬜ 待讨论 |
| D-13 | 是否开源/协议 | ⬜ 待讨论（仓库已 public） |
| D-14 | 双机同步策略（数据/任务） | ⬜ 待讨论 |
| — | 模拟盘两策略资金：各自100万 or 合并200万 | ⬜ 待拍板 |
| — | 若回测跑不赢基准：调参数 or 直接进模拟盘观察 | ⬜ 结果出来后拍板 |

### C 舆情因子（已挂起，C2）

- 已完成股吧数据调研 + 47 个稀疏采样点的最小验证，结论：**单维度月度发帖量不稳健**（暴跌恐慌与牛市狂热都导致发帖暴增，绝对值区分不了）。
- 精细化方向（环比变化率、结合涨跌方向区分狂热/恐慌、日度粒度）已记录于 `docs/strategies/舆情因子与模拟盘设计_v0.1.md`，D 之后再启。
- **本决定（2026-09-20）：股吧采集先挂起，不急着启动。** 它要攒 3–6 个月数据才见效，且价值模拟盘刚起步用不上；不分散现在精力。
- **为什么采（一句话）**：做"散户情绪温度计"当反向指标——股吧狂热(>150)警惕见顶减仓、冷清(<80)关注见底；给价值/ETF策略加情绪过滤。
- **采什么规则（不是全量抓帖）**：每只股票股吧每天只记 4 个快照数（发帖数/评论数/浏览量/活跃用户去重），合成情绪指数 = 近5日均发帖 ÷ 过去60日均发帖 ×100。完整口径见设计文档 §1.3。
- **什么时候启动（触发条件，满足任一即可）**：① 价值模拟盘跑稳 1–3 个月、主策略定型；② 要给策略加"别追高/敢抄底"的情绪过滤时；③ 操盘手主动提出要盯股吧情绪。启动即按设计文档阶段2写东方财富爬虫，后台慢采，攒够再验证。

### 外脑线（已纳入规划，挂起待激活）

- **定位**：项目北极星的另一半——为操盘手（炒股客/nero6688）做"懂他的 AI"。与量子系统并行，不是独立项目。
- **何时激活**：**适当时机**（价值策略模拟盘跑稳、或操盘手本人在场可对话时）启动，当前不主动推进。
- **激活入口**：从 `docs/外脑落地SOP.md` **阶段0** 开始（先摸清他现在怎么用豆包、哪觉得答得没用）。
- **配套文档**：`docs/外脑落地SOP.md`（怎么一步步采）、`docs/操盘手交易判断采集清单.md`（采什么，按打法五要素）、`docs/参与者画像.md` §2.2（他是谁、沟通纪律五条）。
- **采集原则**：不靠他主动总结，由 AI 在日常对话里设计场景问；他答一条，沉淀一条进专属交易档案。
- **被动采集现在就并行（2026-09-20 定）**：不用等"激活"仪式。AI 已掌握历史对话、又在持续接收操盘手输入，所以**每次对话遇到他透露的交易判断，就顺手沉淀进 `操盘手交易判断采集清单.md` / 专属交易档案**。要"激活"的只是阶段0那种"专门约他谈、给他看懂他的AI"的服务流程；采集行为从现在就开始跑。两种做法都接受：边工作边采，或定期重梳历史对话补全。
- **操盘手其它窗口增量采集已上线（2026-09-21，A方案，记忆触发不挂定时）**：脚本 `scripts/scan_operator_sessions.py` + SOP `docs/外脑-操盘手会话增量采集SOP.md`。按 jsonl 行号游标增量扫所有主会话（默认排除当前、噪音过滤、"建仓"作歧义词），命中进 `data/operator_recall/inbox/`，AI 审阅后归档到 `09_调研底稿与素材/操盘手经验/`（编号顺延，已到 125）。本机已 baseline，今后只报新增。**机器边界**：操盘手本人窗口在他自己电脑、trajectory 不跨机同步，脚本须在他机器同仓跑；豆包 2.1Pro 普通对话存云端不落本地，只能走 thread 分享链接。
- **2026-09-18 复盘已归档 125**：当日"放量普涨、弃防御切进攻"，银行/煤炭/高股息小跌、资金流向地产科技。**价值模拟盘 9-21 启动首日恰逢高股息防御板块短期回调**——作为观察"价值策略在风格切换期表现"的起点记录，非买卖建议。

## 会话整理断点（增量记忆指针）

> 用途：用户多次要求"用本地数据库整理本会话全部内容"。为避免每次从头整理，记下上次整理到哪，下次**只从断点之后增量整理**。

- **上次全量整理截止锚点**：2026-09-20，本轮把"第三方对接文档 + 外脑方法论 + G2 回测结果"落地完成。
- **判断标志（当时最后一句话）**：用户说——"把外脑这个事情纳入规划吧，在适当的时机可以激活；同时记录上次让你重新整理数据库、关于这个会话全部内容的时机点，可以记录当时的最后一句话当判断标志，下回再整理时从那之后整理。"
- **下次整理规则**：从上述标志句**之后**的新对话开始增量整理（用 `local-trajectory-recall` 的 `search --id` 或 `export`，跳过本标志句之前的内容）；整理完后，把本锚点和标志句**更新为新一轮的最后一句话**。
- **关联技能**：`~/Doubao/skills/local-trajectory-recall/`（跨会话 trajectory 检索，豆包专属）。

## 已完成（Done，近期）

| # | 任务 | 完成日期 | 产出 |
|---|---|---|---|
| T23 | C 舆情因子数据调研+最小验证 | 2026-09-19 | `results/sentiment_verification/`、爬虫与分析脚本 |
| T24 | baostock 财务数据可得性验证 | 2026-09-19 | 确认 profit/growth/dividend 三类全可得且带发布日（可PIT） |
| T25 | D 多因子策略规格定稿 | 2026-09-19 | `docs/strategies/多因子选股策略_v0.1.md` |
| T22a | D 回测系统代码框架 | 2026-09-19 | `src/quant/` 五模块 + 入口脚本（小样本跑通） |
| T26 | 财务下载挂 launchd（重启自动续） | 2026-09-19 | com.astock-quant.fundamentals（SuccessfulExit=false） |

## 全量行情下载（T13）自愈架构（2026-09-21 重构）

**背景**：下载自 9-20 中断后多次"假死"，总要人工催才继续。9-21 定位三处根因并彻底重构为 launchd 双任务，目标=后台稳定、崩溃/重启自动续、单例、下完为止、无需人工干预。

**真实进度（以磁盘有效 csv 为准，持续增长；自查命令见下）**
- ETF 5分钟线：1588/1588 = 100%（日线也已在 `data/raw/etf`，共 3176 文件）。
- 个股目标 **4889 只**（baostock 全市场，正则留 `sh.6/sz.0/sz.3`、剔科创板 `sh.688`、天然不含北交所）。
- 重构时点：个股日线约 1415/4889（29%）、5分钟线约 1042/4889（21%）；缺口从 sh.603050 连续开始（原中断点）。
- 口径核查：`find ... -name '*688*'` 命中的全是代码含 688 数字的主板股/ETF（如 sh.601688 华泰证券、sh.600688 上海石化），**无真正科创板**；`bj/` 文件数 0。符合"剔科创、不要北交所"。

**三处根因（均已修）**
1. 旧监控毕业判据 `completed+failed>=总数` 把中断造成的**假失败当"处理完"直接退出**，失败股永不重试；且 break 在重启逻辑之前。→ 新监控**只认磁盘文件，failed 不参与毕业**，日线+分钟线 remaining 全为 0 才退出。
2. `download_data.py` 裸 `bs.login()` 无超时，网络异常时无限挂起（重启即死、只打印标题的元凶）。→ 新增 `baostock_login(max_retry=3, timeout=60)`（SIGALRM 包裹、重试、最终失败 exit 2）。
3. failed 列表只增不减、重试成功不移除、重跑重复 append，计数虚高且与磁盘/completed 大量重叠。→ **每轮启动清空上一轮 failed**（磁盘无文件即本轮重试），成功/失败/跳过全部去重、成功即移出 failed。

**关键坑（务必记住）：不要在 launchd 作业里用 `subprocess.Popen` 派生下载孙子进程**——实测该子进程启动几秒内即死且无任何输出（start_new_session 也无效）；手动 nohup 正常。**正确做法是让下载本身成为独立 launchd 任务**。

**双 launchd 任务架构（plist 在 `~/Library/LaunchAgents/`）**
| 任务 Label | 跑什么 | 自愈策略 |
|---|---|---|
| `com.astock-quant.download-data` | `.venv/bin/python scripts/download_data.py --freq both` | RunAtLoad（开机/登录自启）；KeepAlive=`{SuccessfulExit:false}`（崩溃非0自动重启，正常跑完一遍 exit0 不重启）；ThrottleInterval=120；NetworkState=true（联网才跑、断网恢复再触发） |
| `com.astock-quant.download-monitor` | `scripts/download_monitor.py`（磁盘为准统计+守护） | 每 15 分钟巡检：未完成且下载任务没在跑就 `launchctl kickstart` 拉起（任务被卸载也会自动 load 回来）；连续约 5 小时无增长强制 kickstart 一次；全完成才 exit0；自带单例锁 |

- 单例：launchd 每个 Label 天然单例 + 监控 pgrep 去重 + 监控锁文件，保证同一时间只有一个下载进程。
- 进度统计只看文件大小（>2KB 视为有效），近万文件 0.13 秒完成（旧版逐个数全文行数要扫约 9GB、卡几分钟）。
- 行为：**开机登录后自动开跑；合盖休眠期间暂停，开盖/唤醒或重启登录后续跑**；单只网络失败记 failed，下一轮（跑完一遍后由监控 kickstart）自动重试，断点续传跳过已完成。
- 备份：改前两脚本与旧 status JSON 在 `data/_workspace/backup_20260921/`。

**常用运维命令**
```bash
cd ~/Desktop/astock-quant
launchctl list | grep astock-quant                 # 看两个任务 PID（第二列；"-"=未运行）
.venv/bin/python - <<'PY'                          # 看实时磁盘进度
import sys; sys.path.insert(0,"scripts"); import download_monitor as m
t=m.load_target_codes(); p=m.get_progress(t)
for f in ("daily","minute"):
    x=p[f]; print(f, x["done"],"/",x["total"],"待补",x["remaining"],f'({x["pct"]:.1f}%)')
PY
tail -f data/_workspace/download.log               # 下载明细
tail -f data/_workspace/download_monitor.log       # 监控巡检
# 手动停/启：launchctl unload|load ~/Library/LaunchAgents/com.astock-quant.download-data.plist
# 手动拉起（任务已加载但没在跑）：launchctl kickstart gui/$(id -u)/com.astock-quant.download-data
```

**规模预估（baostock 实测）**：日线单只约 7 秒、5分钟线单只约 96–110 秒（约 7.8 万行）。日线剩余约数小时；**5分钟线全量约连续 4–5 天**（主瓶颈）；最终空间日线约 4G + 分钟线约 40G ≈ **44G**（磁盘 2.8T 可用，无压力）。当前月度策略主要用日线、回测池只需中证800（706只）；全量 5 分钟线是为"最完整"预留，**是否缩到中证800以节省 4–5 天/空间，待用户拍板（不影响后台继续跑）**。

**fundamentals 财务任务**：`com.astock-quant.fundamentals` plist 存在但当前 **unload**（中证800 财务已基本下完：profit/growth 各706、dividend704）；避免与行情下载抢连接，需要补财务时再 load。

## 已启用模式

- [x] TASK_STATUS + ISSUES（基础治理）
- [ ] 工程记忆 bundle（项目长大到需要时再加）
- [x] 批量任务执行规范（数据下载 launchd 托管 + 断点续传 + 状态 JSON）
- [x] **数据资产清单随变更同步**（2026-09-20 起）：`docs/数据资产清单.md` 第三节实测数字，随某类下完/进度大跳/新增或剔除数据类型立即更新，口径 `du -sh data/raw/*/` + `data/_workspace/*.json`。
